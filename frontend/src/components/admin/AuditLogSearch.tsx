import { DeferredFeaturePanel } from '@/components/ui/DeferredFeaturePanel'

export function AuditLogSearch() {
  return (
    <DeferredFeaturePanel phaseLabel="Phase 4" title="AI audit log search">
      The admin audit-log console depends on the later AI audit table and search endpoints. This screen reserves the
      final admin information architecture without pretending the governance backend already exists.
    </DeferredFeaturePanel>
  )
}
