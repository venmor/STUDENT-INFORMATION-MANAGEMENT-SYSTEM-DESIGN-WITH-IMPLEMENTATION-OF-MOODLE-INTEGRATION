services:
  db:
    ports:
      - "127.0.0.1:${DEV_DB_PORT:-3313}:3306"

  proxy:
    ports:
      - "127.0.0.1:${DEV_HTTP_PORT:-8080}:80"

  qdrant:
    ports:
      - "127.0.0.1:${QDRANT_HTTP_PORT:-6333}:6333"
